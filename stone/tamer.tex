%%% NOTE: spaces inside command body matter

\usepackage[total={6.5in,8.75in}, top=1.2in, left=0.9in, includefoot]{geometry}

%% NOTE: Scribble does not number sections whose depth is larger than 3
\setcounter{tocdepth}{4} % it seems that this line is redundant

%% By default, hyperref drops positions of tags and jumps to the containing section
%%  or where the nearest `refstepcounter{...}` put.
%% All examples are anchored with the counter "example" though latex counter itself is useless.
\newcounter{example}

%%% Ruby Styles
\usepackage{luatexja-fontspec}
\usepackage{luatexja-ruby}

\ltjsetruby{size=0.6}
\ltjsetruby{mode=00}

\newcommand{\rubyterm}[2]{\ruby[size=0.50]{#1}{#2}}
\newcommand{\bigruby}[2]{\ruby[size=0.75]{#1}{#2}}
\newcommand{\exmptag}[2]{\ruby[size=0.75,intergap=-2.0,rubysmash=false]{#1}{#2}}
\newcommand{\exmpruby}[3]{\ruby[size=0.75,intergap=0.1,stretchruby=#3]{#1}{#2}}

% stupid latex as its commands' names can only contain letters
\newcommand{\exmprubyvl}[2]{\exmpruby{#1}{#2}{001}}
\newcommand{\exmprubyvc}[2]{\exmpruby{#1}{#2}{101}}
\newcommand{\exmprubyvr}[2]{\exmpruby{#1}{#2}{100}}

\newcommand{\Font}[2]{\jfontspec{#1} #2}

%%% Literacy Styles
\renewcommand{\boxed}[1]{\ColorBox{\textsmaller{#1}}{AliceBlue}}

\newcommand{\jaexbox}[1]{\PlainBox{#1}}
\newcommand{\jatech}[1]{\color{Black}{#1}}

% For typesetting IPA Phonetic Symbols
\usepackage[tone,extra]{tipa}
\usepackage{tipx}

\newcommand{\iparuby}[3]{\ruby[size=0.75,intergap=#3]{#1}{#2}}
\newcommand{\ipadoublebar}[1]{\ipabar{#1}{0.618ex}{1.2}{}{}}

\newcommand{\ipacentrialized}[1]{\textipa{\"#1}}
